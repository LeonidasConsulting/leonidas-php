# Secrets — upload to GoDaddy directly via SFTP, never commit
includes/config.php
chat/api.php
chat/blog-api.php
.google_ai_key
.claude/commands/php-rewrite.md

# Build artifacts — node_modules never committed
node_modules/
package-lock.json

# Dev tools — not for web root
tools/
generate_batch2.py
tailwind.config.js
.claude/

# Rate limit files (generated at runtime, never commit)
data/rl_*.json

# OS / editor
.DS_Store
Thumbs.db
.vscode/
*.log
*.lock
